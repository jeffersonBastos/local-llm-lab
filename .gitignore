# Secrets / machine-specific — never commit
.env
.claude/settings.local.json
CLAUDE.local.md

# Internal log — feeds a LinkedIn post draft, not for the public repo
DEVLOG.md

# Benchmark scratch
benchmarks/.work/
__pycache__/
*.pyc
.DS_Store

# local tooling pin
.tool-versions
local-model-test/
local-model-test-2/
